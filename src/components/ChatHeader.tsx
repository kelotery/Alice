import React from "react";
import { useConversation } from "../store/conversationStore";

const ChatHeader: React.FC = () => (
  <div className="chat-header">与Marvis的对话</div>
);

export default ChatHeader;
